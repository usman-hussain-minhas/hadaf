{
  "template_id": "H04-F04-record-output-template",
  "purpose": "Public-safe fixture template for deterministic record generator output.",
  "required_fields": [
    "schema_version",
    "record_id",
    "durable_ref"
  ]
}
